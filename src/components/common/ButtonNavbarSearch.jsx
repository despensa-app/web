import Button from "./button/Button";

const ButtonNavbarSearch = ({onClick}) => (
    <Button onClick={onClick}>
        <i className="fas fa-search"/>
    </Button>
);

export default ButtonNavbarSearch;